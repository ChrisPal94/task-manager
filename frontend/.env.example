# API base URL — only needed when frontend and backend are on different origins
# Leave empty if CloudFront proxies /api/* to the backend (recommended)
# VITE_API_BASE_URL=https://your-backend-url.com
